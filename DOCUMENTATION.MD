# 📖 เอกสารประกอบโครงการ: สวิฟท์ยูไอ 🇹🇭

เอกสารนี้ให้ **ภาพรวมที่มีรายละเอียด** ของ **องค์ประกอบภาษาไทยที่กำหนดเอง** ที่นิยามในโครงการ, โดยหลักแล้วภายในไฟล์ `สวิฟต์ยูไอไทย.swift`.

---

## 1. นามแฝงชนิด (Type Aliases) (การกำหนดนามแฝงและ Types ภาษาไทย)

เราใช้ `typealias` เพื่อ **แผนที่ชนิดพื้นฐานของสวิฟท์** และ **พิธีสาร** ไปยัง **เทียบเท่าแนวคิด** ที่ใกล้ที่สุดของพวกเขาในภาษาไทย.

| องค์ประกอบ ดั้งเดิม | นามแฝง ไทย | ชนิด/พิธีสาร สวิฟท์ | ความหมาย (คำแปลตรงตัว ไทย) |
| :--- | :--- | :--- | :--- |
| `View` | `มุมมอง` | `View` | มุมมอง |
| `Color` | `สี` | `Color` | สี |
| `CGFloat` | `หน่วยทศนิยม` | `CGFloat` | หน่วยเลขทศนิยม |
| `Int` | `จำนวนเต็ม` | `Int` | จำนวนการนับเต็ม |
| `Text` | `ข้อความ` | `Text` | ข้อความ |
| `Image` | `รูปภาพ` | `Image` | ภาพ |
| `Button` | `ปุ่ม` | `Button` | ปุ่ม |
| `Spacer` | `ตัวเว้นระยะ` | `Spacer` | ตัวแทนการเว้นช่องว่าง |
| `Alignment` | `การจัดตำแหน่ง` | `Alignment` | การวางตำแหน่ง |
| `HorizontalAlignment` | `การจัดแนวนอน` | `HorizontalAlignment` | การจัดเรียงในแนวนอน |
| `VerticalAlignment` | `การจัดแนวตั้ง` | `VerticalAlignment` | การจัดเรียงในแนวตั้ง |

---

## 2. ตัวห่อหุ้มโครงสร้าง (Structural Wrappers) (Components โครงสร้าง ภาษาไทย)

เพราะ **มุมมองภาชนะ** เหมือน `VStack`, `HStack`, และ `ZStack` **มักจะต้องการ** ป้ายกำกับ **พารามิเตอร์เฉพาะ** (เหมือน `alignment` และ `spacing`), เราใช้ **ตัวห่อหุ้มโครงสร้าง** ที่กำหนดเองเพื่อยอมให้มีการใช้ **ป้ายกำกับพารามิเตอร์ภาษาไทย** ใน **ตัวริเริ่ม** ขณะที่ยังคงเรียก **โครงสร้างสวิฟท์ยูไอที่เป็นดั้งเดิม** ข้างล่าง.

### **2.1. `เรียงแนวตั้ง` (VStack)**

* **วัตถุประสงค์:** จัดวางมุมมอง **ในแนวตั้ง**.
* **ดั้งเดิม:** `VStack(alignment: .center, spacing: 20)`
* **การปฏิบัติการ ไทย:**
    ```swift
    struct เรียงแนวตั้ง<Content: มุมมอง>: มุมมอง {
        // ... (รายละเอียดการปฏิบัติการ)
        init(การจัดเรียง: การจัดแนวนอน = .กลาง, ระยะห่าง: หน่วยทศนิยม? = nil, @ViewBuilder เนื้อหา: () -> Content)
        // ...
    }
    ```
* **พารามิเตอร์หลัก:**
    * `การจัดเรียง` (`alignment`): ใช้ `การจัดแนวนอน` (จัดเรียงในแนวนอน).
    * `ระยะห่าง` (`spacing`): ใช้ `CGFloat` (`หน่วยทศนิยม`).

### **2.2. `เรียงแนวนอน` (HStack)**

* **วัตถุประสงค์:** จัดวางมุมมอง **ในแนวนอน**.
* **พารามิเตอร์หลัก:**
    * `การจัดเรียง` (`alignment`): ใช้ `การจัดแนวตั้ง` (จัดเรียงในแนวตั้ง).
    * `ระยะห่าง` (`spacing`): ใช้ `CGFloat` (`หน่วยทศนิยม`).

### **2.3. `ซ้อนทับ` (ZStack)**

* **วัตถุประสงค์:** **ซ้อนทับ** มุมมองบนยอดของกันและกัน (การซ้อนตามแกน Z).
* **พารามิเตอร์หลัก:**
    * `การจัดเรียง` (`alignment`): ใช้ `การจัดตำแหน่ง`.

---

## 3. ตัวปรับปรุงมุมมอง (View Modifiers) (Extension สำหรับ Modifier ภาษไทย)

เรา **ขยาย** พิธีสาร `มุมมอง` (`View`) เพื่อจัดหา **ชื่อที่เป็นท้องถิ่น** สำหรับตัวปรับปรุงสวิฟท์ยูไอ **ทั่วไป**.

| ตัวปรับปรุง ดั้งเดิม | นามแฝง ตัวปรับปรุง ไทย | คำอธิบาย | ตัวอย่าง |
| :--- | :--- | :--- | :--- |
| `.padding()` | `.ระยะขอบ()` | ตั้งค่า **การบุรอง/ขอบขรึม** ของมุมมอง. | `.ระยะขอบ(30.0)` |
| `.frame()` | `.กรอบ()` | ตั้งค่า **ความกว้างและความสูง** ของมุมมอง. | `.กรอบ(width: 150.0)` |
| `.foregroundStyle()` | `.สีด้านหน้า()` | ตั้งค่า **สีสำหรับเนื้อหา** ของมุมมอง. | `.สีด้านหน้า(สี.มิ้นท์)` |
| `.font()` | `.รูปแบบอักษร()` | ตั้งค่า **รูปแบบข้อความ**. | `.รูปแบบอักษร(.หัวข้อรอง)` |
| `.tint()` | `.สีเน้น()` | ตั้งค่า **สีเน้น**, โดยเฉพาะสำหรับปุ่ม. | `.สีเน้น(สี.แดง)` |
| `.opacity()` | `.ความทึบแสง()` | ตั้งค่า **ระดับความโปร่งใส**. | `.ความทึบแสง(0.3)` |

### **ตัวปรับปรุงที่มีความเฉพาะเจาะจง**

* **นามแฝง รูปแบบปุ่ม:**
    * `.ปุ่มสไตล์เน้นขอบเขต()`: นามแฝง `.buttonStyle(.borderedProminent)`.
    * `.ปุ่มสไตล์ขอบเขต()`: นามแฝง `.buttonStyle(.bordered)`.

* **ตัวริเริ่ม รูปภาพ:**
    * `รูปภาพ(ชื่อระบบ: String)`: นามแฝง **ตัวริเริ่ม** `Image(systemName: String)`.

---

## 4. นามแฝงค่าคงที่ (Constant Aliases) (นามแฝงสำหรับค่าคงที่)

**ค่าคงที่ที่ใช้กันทั่วไป** ก็ถูก **ทำเป็นนามแฝง** สำหรับการทำให้เป็นท้องถิ่นที่ **สมบูรณ์**.

| ค่าคงที่ ดั้งเดิม | นามแฝง ไทย | คำอธิบาย | ตัวอย่างการใช้ |
| :--- | :--- | :--- | :--- |
| `.bottom` (Edge.Set) | `.ล่าง` | ขอบล่าง. | `.ระยะขอบ(.ล่าง, 20.0)` |
| `.mint` (Color) | `.มิ้นท์` | สีมิ้นท์. | `สี.มิ้นท์` |
| `.secondary` (Color) | `.รอง` | สีรอง. | `สี.รอง` |
| `.bold` (Font.Weight) | `.หนา` | น้ำหนักตัวอักษรหนา. | `.ระบบ(weight: .หนา)` |
| `.rounded` (Font.Design) | `.มน` | การออกแบบตัวอักษรโค้งมน. | `.ระบบ(design: .มน)` |
| `.center` (Alignment) | `.กลาง` | การจัดตำแหน่งกึ่งกลาง. | `เรียงแนวตั้ง(การจัดเรียง: .กลาง)` |

---

## 5. ตัวอย่างการใช้งาน (Example Usages)

ส่วนนี้แสดงให้เห็นว่า **นามแฝงไทย** ถูก **รวมกัน** อย่างไรเพื่อสร้าง **มุมมองที่เป็นท้องถิ่น**, โดยใช้รหัสจาก `คอนเท้นท์วิว.swift`.

### 5.1. การนิยามสถานะและโครงสร้างมุมมอง

**ตัวแปรสถานะ** ใช้ **นามแฝง** `Int`, และ **การจัดวางหลัก** ใช้ **ตัวห่อหุ้มกองแนวตั้ง**.

| สวิฟท์ยูไอ ดั้งเดิม | รหัสที่เป็นท้องถิ่น ไทย | คำอธิบาย |
| :--- | :--- | :--- |
| `struct ContentView: View` | `struct คอนเท้นท์วิว: มุมมอง` | นิยาม **โครงสร้างมุมมองหลัก**. |
| `@State private var count: Int = 0` | `@State private var นับจำนวน: จำนวนเต็ม = 0` | นิยาม **ตัวแปรสถานะหนึ่ง** สำหรับการนับ. |
| `var body: some View { ... }` | `var body: some มุมมอง { ... }` | นิยาม **ตัวมุมมอง**. |
| `VStack(alignment: .center, spacing: 20.0) { ... }` | `เรียงแนวตั้ง(การจัดเรียง: .กลาง, ระยะห่าง: 20.0) { ... }` | เริ่มต้น **กองแนวตั้ง** ด้วย **การจัดเรียงกึ่งกลาง** และ **20 จุดของการเว้นระยะห่าง**. |

### 5.2. การรวมตัวปรับปรุง (ข้อความและรูปภาพ)

ตัวอย่างนี้จัดแสดง **ส่วนประกอบการซ้อนทับ** (`ซ้อนทับ`), **นามแฝงตัวริเริ่มรูปภาพ** (`รูปภาพ(ชื่อระบบ:)`), และ **นามแฝงตัวปรับปรุงที่แตกต่างกัน**.

| สวิฟท์ยูไอ ดั้งเดิม | รหัสที่เป็นท้องถิ่น ไทย | คำอธิบาย |
| :--- | :--- | :--- |
| `ZStack { ... }` | `ซ้อนทับ { ... }` | เริ่มต้น **กองชั้น** (ZStack). |
| `Image(systemName: "circle.fill")` | `รูปภาพ(ชื่อระบบ: "circle.fill")` | **ริเริ่ม** รูปภาพของระบบ. |
| `.resizable()` | `.ปรับขนาดได้()` | ยอมให้ **รูปภาพถูกปรับขนาด**. |
| `.frame(width: 150.0)` | `.กรอบ(width: 150.0)` | ตั้งค่า **กรอบความกว้าง/ความสูง** ที่ชัดเจน. |
| `.foregroundStyle(Color.mint)` | `.สีด้านหน้า(สี.มิ้นท์)` | ตั้งค่า **รูปแบบสีหลัก**. |
| `Text("\(count)")` | `ข้อความ("\(นับจำนวน)")` | แสดง **ตัวแปรนับปัจจุบัน**. |
| `.font(.system(size: 48, weight: .bold, design: .rounded))` | `.รูปแบบอักษร(.ระบบ(size: 48.0, weight: .หนา, design: .มน))` | ตั้งค่า **คุณสมบัติตัวอักษรที่กำหนดเอง** โดยใช้ **นามแฝงที่เป็นท้องถิ่น**. |
| `.padding(.bottom, 20.0)` | `.ระยะขอบ(.ล่าง, 20.0)` | ตั้งค่า **การบุรอง** เฉพาะบน **ขอบล่าง** โดยใช้ **นามแฝง** `.ล่าง`. |

### 5.3. การปฏิบัติการปุ่ม (Button Implementation)

**องค์ประกอบแบบโต้ตอบ** ใช้ **นามแฝงปุ่ม** (`ปุ่ม`), **กองแนวนอน** (`เรียงแนวนอน`), และ **นามแฝงรูปแบบปุ่มที่กำหนดเอง**.

| สวิฟท์ยูไอ ดั้งเดิม | รหัสที่เป็นท้องถิ่น ไทย | คำอธิบาย |
| :--- | :--- | :--- |
| `HStack(spacing: 30.0) { ... }` | `เรียงแนวนอน(ระยะห่าง: 30.0) { ... }` | เริ่มต้น **กองแนวนอน** ด้วย **30 จุดของการเว้นระยะห่าง**. |
| `Button("Add") { ... }` | `ปุ่ม("เพิ่ม") { ... }` | นิยาม **ปุ่มหนึ่ง** ด้วยป้ายกำกับ "**เพิ่ม**." |
| `.buttonStyle(.borderedProminent)` | `.ปุ่มสไตล์เน้นขอบเขต()` | ใช้ **รูปแบบปุ่มที่เน้น**. |
| `Button("Reset") { ... }` | `ปุ่ม("รีเซ็ต") { ... }` | นิยาม **ปุ่มหนึ่ง** ด้วยป้ายกำกับ "**ตั้งค่าใหม่**." |
| `.buttonStyle(.bordered)` | `.ปุ่มสไตล์ขอบเขต()` | ใช้ **รูปแบบปุ่มที่มีขอบเขต**. |
| `.tint(Color.red)` | `.สีเน้น(สี.แดง)` | ตั้งค่า **สีเน้น** สำหรับปุ่ม. |
